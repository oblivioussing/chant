export default {
{{#each list}}
  {{#if dict}}
  // {{columnComment}}
  {{columnName}}: {
    {{#each dict}}
    {{@key}}: '{{this}}'{{#if @last}}{{else}},{{/if}}
    {{/each}}
  },
  {{/if}}
{{/each}}
}
