import { type {{upper module}} } from '@prisma/client'

export const {{upper module}}Entity = {
  id: '', // id
  {{#each list}}
  {{columnName}}: {{valueType columnType}}{{comma @last}} // {{columnComment}} {{dictNote dict}}
  {{/each}}
} satisfies Partial<{{upper module}}>

export const {{upper module}}Dto = {
  ...{{upper module}}Entity
}

export const {{upper module}}Vo = {
  ...{{upper module}}Entity
}

