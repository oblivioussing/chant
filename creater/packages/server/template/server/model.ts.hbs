import { Prisma, type {{upper module}} } from '@prisma/client'

export const {{module}}Base = {
  id: '', // id
  {{#each list}}
  {{columnName}}: {{valueType columnType}}{{comma @last}} // {{columnComment}} {{dictNote dict}}
  {{/each}}
}

export const {{module}}Entity = {
  ...{{module}}Base,
  createId: '', // 创建人id
  createTime: new Date(), // 创建时间
  updateId: '', // 更新人id
  updateTime: new Date() // 更新时间
} as {{upper module}}

export const {{module}}Vo = {
  ...{{module}}Base
}

export type {{upper module}}Base = typeof {{module}}Base
export type {{upper module}}Vo = typeof {{module}}Vo
